# Repository Layer

This package is reserved for database access code.

Keep SQLAlchemy query composition, pagination queries, and transaction-local data access here when service logic becomes complex enough to separate from `backend/services/`.
